These scripts are mainly for the purpose of sharing the configurations (parallelism and model specific) that we ran for the workshop.
These files should be in MEGATRON_ROOT directory for direct usage.
They will fail otherwise


The runs are based on a specifc commit of Megatron-LM, which was modified later.
## Runs based on Megatron upstream repo commit c7a1f82
## THIS IS REQUIRED 
# RUN git clone -b codep_settings  https://github.com/schheda1/Megatron-LM.git
